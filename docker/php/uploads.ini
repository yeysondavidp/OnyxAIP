; Matches the app's photo validation ceiling (JobFlowController: max:20480 KB)
; plus headroom so PHP's own limit isn't the thing rejecting an in-range file.
upload_max_filesize=21M
post_max_size=25M
